import React, { useState } from 'react'
import ReactDom from 'react-dom'
import s from 'styled-components'
import Comment from './Comment'

const StyledDiv = s.div`
  margin-bottom: 20px;
  padding-top: 38px;
`

const Posts = ({posts}) => <div></div>

export default Posts